#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() 
{
 char a[200],b[200],c[2000];
  gets(a);
    gets(b);
    gets(c);
    printf("%s\n",a);
    printf("%s\n",b);
    printf("%s",c);
    return 0;
}