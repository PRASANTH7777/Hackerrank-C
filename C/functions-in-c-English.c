#include stdio.h

Add `int max_of_four(int a, int b, int c, int d)` here.


int main() 
{
    int a, b, c, d;
    scanf(%d %d %d %d, &a, &b, &c, &d);
    if(ab&&ac&&ad)
    { 
        printf(%d,a);
    }
      else if(bc&&bd)
      {
          printf(%d,b);
      }
    else if(cd)
      {
          printf(%d,c);
      }
    else
      {
          printf(%d,d);
      }
       

    
    return 0;
}